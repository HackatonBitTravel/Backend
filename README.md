# Backend
Core backend service that manages authentication, users, transactions, and business logic for the platform. Exposes REST endpoints for the frontend and integrates with the AI API. Built with FastAPI (Python),  it ensures secure and efficient data exchange across all services.
